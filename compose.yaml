services:
  build:
    platform: ${BUILD_PLATFORM:-linux/amd64}
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      RUNTIME: ${RUNTIME:-renpy8}
    volumes:
      - type: bind
        source: ${OUTPUT_DIR:-./artifacts}
        target: /artifacts
